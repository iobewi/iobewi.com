<header class="site-header">
  <div class="container header-row">
    <a class="brand" href="/">IOBEWI</a>
    <nav class="nav">
      {% for item in nav %}
        <a class="nav-link" href="{{ item.url }}">{{ item.label }}</a>
      {% endfor %}
    </nav>
    <a class="btn btn-primary header-cta" href="{{ theme.ctaPrimary.url }}">
      {{ theme.ctaPrimary.label }}
    </a>
  </div>
</header>